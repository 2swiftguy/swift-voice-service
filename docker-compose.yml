services:
  voice:
    build: .
    working_dir: /app
    command: uvicorn main:app --host 0.0.0.0 --port 8001 --reload
    volumes:
      - .:/app:cached
    env_file: .env.local
    ports:
      - "127.0.0.1:8001:8001"
    restart: unless-stopped